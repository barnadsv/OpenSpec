name: sdd-plus-superpowers
version: 1
description: >
  Spec-driven workflow integrated with Superpowers skills.
  intent (optional) → brainstorm → proposal → design/specs → tasks → plan.
  Apply phase uses git worktrees + subagent-driven development.

artifacts:
  - id: intent
    generates: intent.md
    description: Normalized input from any source (PRD, brainstorm, ticket, conversation). Bridges the gap between raw ideas and structured planning.
    template: intent.md
    optional: true
    instruction: |
      Create or refine the intent document that captures the normalized
      input for this change. The intent.md acts as a universal entry point
      that accepts input from any source (PRD, external brainstorm,
      ticket, conversation) and distills it into a standard format.

      If the user provides a PRD or external document as source:
      - Extract the core problem, desired outcome, and scope
      - Preserve decisions already made (do NOT re-explore them)
      - Discard implementation prescriptions — keep the "what" and "why",
        drop the "how"
      - Reference the source document in the Source section

      If the user provides a raw idea or conversation notes:
      - Capture what is known, flag what is not
      - List open questions explicitly

      CRITICAL decision logic for downstream flow:
      - If "Open questions" is EMPTY → brainstorm can be skipped.
        The proposal artifact should consume intent.md directly.
      - If "Open questions" has items → brainstorm is required
        to resolve them before proceeding to proposal.

      Keep it concise (under 1 page). This is a filter, not a document.
    requires: []

  - id: brainstorm
    generates: brainstorm.md
    description: Collaborative design exploration using Superpowers brainstorming
    template: brainstorm.md
    instruction: |
      Use the Skill tool to invoke **superpowers:brainstorming**.

      **If intent.md exists in this change directory**, pass its content
      as context to the brainstorming session. Specifically:
      - Use "Problem" and "Desired outcome" to frame the discussion
      - Use "Decisions already made" as constraints (do not re-explore)
      - Use "Open questions" as the agenda — these are the items
        that need resolution through brainstorming
      - Reference "Source" for additional context if needed

      **If intent.md does not exist**, proceed with standard
      brainstorming from scratch.

      IMPORTANT output redirection:
      - Do NOT write to `docs/superpowers/specs/`. Instead, write the
        brainstorming design output directly to this change's `brainstorm.md`.
      - If brainstorming produces a design document, also write it to
        this change's `design.md` (this will pre-fill the design artifact).

      After brainstorming completes, summarize the agreed design in
      brainstorm.md using the template structure.

      The brainstorming skill will:
      1. Explore project context
      2. Ask clarifying questions (one at a time)
      3. Propose 2-3 approaches with trade-offs
      4. Present design sections for approval
      5. Output the validated design
    requires: []

  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: |
      Create the proposal document based on available upstream artifacts.

      **Source resolution order:**
      1. If brainstorm.md exists → use it as primary source (standard flow)
      2. If only intent.md exists (brainstorm was skipped) → use intent.md
         as primary source. Extract "Problem" as the Why, "Desired outcome"
         and "Scope" as What Changes, and "Decisions already made" as
         constraints for the design.

      From the resolved source, extract:
      - **Why**: The motivation
      - **What Changes**: Specific changes agreed upon
      - **Capabilities**: Identify which specs will be created or modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<name>/spec.md`. Use kebab-case names.
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Check `openspec/specs/` for existing spec names.
      - **Impact**: Affected code, APIs, dependencies, or systems.

      Since upstream artifacts already explored the problem space,
      this should be a concise extraction, not a new exploration.

      IMPORTANT: The Capabilities section is critical. It creates the contract between
      proposal and specs phases. Each capability listed here will need a spec file.

      Keep it concise (1-2 pages). Focus on the "why" not the "how".
    requires:
      - brainstorm

  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    instruction: |
      Create the design document explaining HOW to implement the change.

      Check if brainstorming already produced a design.md file.
      If design.md already exists with content from brainstorming,
      review and refine it rather than starting from scratch.

      If design.md is empty or doesn't exist, create it from
      the brainstorm.md output.

      Sections:
      - **Context**: Background, current state, constraints, stakeholders
      - **Goals / Non-Goals**: What this design achieves and explicitly excludes
      - **Decisions**: Key technical choices with rationale (why X over Y?). Include alternatives considered.
      - **Risks / Trade-offs**: Known limitations, things that could go wrong. Format: [Risk] → Mitigation
      - **Migration Plan**: Steps to deploy, rollback strategy (if applicable)
      - **Open Questions**: Outstanding decisions or unknowns to resolve

      Focus on architecture and approach, not line-by-line implementation.
      Good design docs explain the "why" behind technical decisions.
    requires:
      - brainstorm

  - id: specs
    generates: "specs/**/*.md"
    description: Detailed specifications for the change
    template: spec.md
    instruction: |
      Create specification files that define WHAT the system should do.

      Create one spec file per capability listed in the proposal's Capabilities section.
      - New capabilities: use the exact kebab-case name from the proposal (specs/<capability>/spec.md).
      - Modified capabilities: use the existing spec folder name from openspec/specs/<capability>/ when creating the delta spec at specs/<capability>/spec.md.

      Delta operations (use ## headers):
      - **ADDED Requirements**: New capabilities
      - **MODIFIED Requirements**: Changed behavior - MUST include full updated content
      - **REMOVED Requirements**: Deprecated features - MUST include **Reason** and **Migration**
      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format

      Format requirements:
      - Each requirement: `### Requirement: <name>` followed by description
      - Use SHALL/MUST for normative requirements (avoid should/may)
      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format
      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3 hashtags or bullets will fail silently.
      - Every requirement MUST have at least one scenario.

      Specs should be testable - each scenario is a potential test case.
    requires:
      - proposal

  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: |
      Create the task list that breaks down the implementation work.

      **IMPORTANT: Follow the template below exactly.** The apply phase parses
      checkbox format to track progress. Tasks not using `- [ ]` won't be tracked.

      Guidelines:
      - Group related tasks under ## numbered headings
      - Each task MUST be a checkbox: `- [ ] X.Y Task description`
      - Tasks should be small enough to complete in one session
      - Order tasks by dependency (what must be done first?)

      Reference specs for what needs to be built, design for how to build it.
      Each task should be verifiable - you know when it's done.
    requires:
      - specs
      - design

  - id: plan
    generates: plan.md
    description: Micro-task implementation plan using Superpowers writing-plans
    template: plan.md
    instruction: |
      Use the Skill tool to invoke **superpowers:writing-plans**.

      IMPORTANT output redirection:
      - Do NOT write to `docs/superpowers/plans/`. Instead, write
        the plan directly to this change's `plan.md`.

      The writing-plans skill will:
      1. Read the tasks.md and design.md for context
      2. Break each task into 2-5 minute micro-steps (TDD style)
      3. Include exact file paths, code snippets, test commands
      4. Add commit points after each task

      Pass the tasks.md content to the skill as the input for
      decomposition. The plan should reference the specs and
      design artifacts in this change directory.
    requires:
      - tasks

apply:
  requires: [plan]
  tracks: tasks.md
  instruction: |
    Before implementing, set up an isolated workspace and executor:

    1. **Workspace**: Use the Skill tool to invoke
       **superpowers:using-git-worktrees** to create an isolated
       git worktree for this change.

    2. **TDD enforcement**: Use the Skill tool to invoke
       **superpowers:test-driven-development**. This is MANDATORY
       for every task — no exceptions.

       Every micro-task in plan.md MUST follow the Red-Green-Refactor cycle:
       - **RED**: Write a failing test FIRST that captures the requirement
         from the corresponding spec scenario. Run the test. It MUST fail.
         If it passes, the test is not testing anything new — rewrite it.
       - **GREEN**: Write the MINIMUM code to make the test pass.
         No extra logic, no premature abstractions. Run the test. It MUST pass.
       - **REFACTOR**: Clean up code and tests while keeping all tests green.
         Run the full test suite after refactoring.

       HARD RULES:
       - Do NOT write implementation code before a failing test exists.
       - Do NOT skip the RED phase — a test that never failed proves nothing.
       - Do NOT move to the next task until all tests for the current task pass.
       - If a task cannot be tested (infrastructure, config), document
         the reason in a comment and get explicit user approval before proceeding.

    3. **Executor**: Use the Skill tool to invoke
       **superpowers:subagent-driven-development** to execute
       the plan.md micro-tasks with fresh subagents per task.

       Tell the executor:
       - Read plan.md in this change directory for micro-tasks
       - Follow the TDD cycle from step 2 for EVERY task
       - Update tasks.md checkboxes as coarse tasks complete
       - Work within the created worktree
       - Each subagent MUST run the full test suite before marking
         a task as complete

    4. **Completion**: When all tasks are done, use the Skill tool
       to invoke **superpowers:finishing-a-development-branch**
       for branch cleanup, PR creation, etc.

       Before finishing, verify:
       - All tests pass (full suite, not just new tests)
       - No skipped or pending tests remain without documented justification
       - Test coverage for new code meets project standards

    If any skill is unavailable, fall back to manual implementation
    using the standard task-by-task loop from tasks.md,
    but the TDD cycle (Red-Green-Refactor) is STILL MANDATORY
    even in fallback mode.
